# 前后端通信数据流可视化

## 1️⃣ 整体架构图

```
┌──────────────────────────────────────────────────────────────────┐
│                        浏览器 (Browser)                           │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   前端 React 应用                            │  │
│  │           http://localhost:5173 (Vite)                     │  │
│  │                                                            │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │                  ImportPage.tsx                      │  │  │
│  │  │  ┌─────────────────────────────────────────────┐   │  │  │
│  │  │  │ 📁 导入 Excel (本地处理)                     │   │  │  │
│  │  │  │ • XLSX.read()                               │   │  │  │
│  │  │  │ • setProducts()                             │   │  │  │
│  │  │  └─────────────────────────────────────────────┘   │  │  │
│  │  │  ┌─────────────────────────────────────────────┐   │  │  │
│  │  │  │ ✏️  ProductForm.tsx (新增：手动输入)        │   │  │  │
│  │  │  │ • name, brand, category, ...                │   │  │  │
│  │  │  │ • handleAddProduct()                        │   │  │  │
│  │  │  └─────────────────────────────────────────────┘   │  │  │
│  │  │  ┌─────────────────────────────────────────────┐   │  │  │
│  │  │  │ 🎨 生成草稿 (→ 后端)                        │   │  │  │
│  │  │  │ • POST /api/generate-batch                  │   │  │  │
│  │  │  │ • setResults()                              │   │  │  │
│  │  │  └─────────────────────────────────────────────┘   │  │  │
│  │  │  ┌─────────────────────────────────────────────┐   │  │  │
│  │  │  │ 📝 ResultCard.tsx (编辑、收藏、下载)         │   │  │  │
│  │  │  │ • handleEditResult()                        │   │  │  │
│  │  │  │ • handleSaveTemplate() → POST /api/templates│   │  │  │
│  │  │  │ • downloadImage()                           │   │  │  │
│  │  │  └─────────────────────────────────────────────┘   │  │  │
│  │  │  ┌─────────────────────────────────────────────┐   │  │  │
│  │  │  │ 📚 TemplateManager.tsx (模板库管理)          │   │  │  │
│  │  │  │ • GET /api/templates                        │   │  │  │
│  │  │  │ • POST/PUT/DELETE /api/templates/:id        │   │  │  │
│  │  │  └─────────────────────────────────────────────┘   │  │  │
│  │  │  ┌─────────────────────────────────────────────┐   │  │  │
│  │  │  │ 💾 导出 CSV (本地处理)                      │   │  │  │
│  │  │  │ • XLSX.utils.json_to_sheet()               │   │  │  │
│  │  │  │ • XLSX.writeFile()                          │   │  │  │
│  │  │  └─────────────────────────────────────────────┘   │  │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  │                                                            │  │
│  │  状态管理 (React Hooks):                                  │  │
│  │  • products[]                （导入或手动添加的商品）     │  │
│  │  • results[]                 （后端生成的结果）         │  │
│  │  • editedResults Map          （前端编辑后的结果）       │  │
│  │  • templates[] (localStorage) （收藏的模板）           │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼  ▼   │
│  HTTP REST API (JSON)  5 个端点的双向通信                       │
│  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲  ▲   │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
                                  │
                    HTTP 通信 (CORS 已启用)
                                  │
┌──────────────────────────────────────────────────────────────────┐
│                        Node.js 服务器                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │           Express Mock API Server                          │  │
│  │           http://localhost:3000                            │  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ 1. POST /api/generate-batch                          │ │  │
│  │  │    输入: { items: Product[], options: {...} }        │ │  │
│  │  │    处理: generateDraftForItem() × N                   │ │  │
│  │  │    输出: { results: GenerateResult[] }               │ │  │
│  │  │                                                       │ │  │
│  │  │    ├─ 规则引擎: 字段 → 标题/卖点                    │ │  │
│  │  │    ├─ 模板套用: {name}, {brand} → 替换              │ │  │
│  │  │    └─ SVG 合成: Canvas → base64                      │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ 2. GET /api/templates                                │ │  │
│  │  │    输入: 无                                           │ │  │
│  │  │    处理: 返回 templates[]                            │ │  │
│  │  │    输出: { templates: Template[] }                   │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ 3. POST /api/templates                                │ │  │
│  │  │    输入: { name, tags, parts }                        │ │  │
│  │  │    处理: templates.push(新模板)                       │ │  │
│  │  │    输出: { template: Template }                       │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ 4. PUT /api/templates/:id                            │ │  │
│  │  │    输入: { name, tags, parts }                        │ │  │
│  │  │    处理: 更新指定模板                                │ │  │
│  │  │    输出: { template: Template }                       │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │ 5. DELETE /api/templates/:id                         │ │  │
│  │  │    输入: id                                           │ │  │
│  │  │    处理: 删除指定模板                                │ │  │
│  │  │    输出: { success: true }                            │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  │  内存存储:                                               │  │
│  │  • let templates = []  （模板库）                       │  │
│  │  • 生成结果直接返回（不存储）                            │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2️⃣ 数据流向细节

### 流程 A: 导入 → 生成 → 编辑 → 导出

```
用户操作                  前端处理                后端处理
   │                       │                      │
   │─上传 Excel─────────→ FileReader              │
   │                      XLSX.read()             │
   │                      setProducts()           │
   │                      ↓ Product[]             │
   │─点击"生成草稿"──────→ fetch POST             │
   │                      {items, options}  ──────→ generateDraftForItem()
   │                                         (5个)   generateDraftForItem()
   │                                         (5个)   generateDraftForItem()
   │                      ↑                  ←────  { results: [...] }
   │                      setResults()            │
   │─查看结果卡片  ──────→ ResultCard 组件        │
   │                                               │
   │─编辑标题/卖点 ──────→ handleEditResult()     │
   │                      (本地修改)               │
   │                      editedResults.set()      │
   │─收藏为模板  ────────→ fetch POST             │
   │                      /api/templates  ───────→ templates.push()
   │                                        ←──── { template }
   │                      localStorage.setItem()   │
   │─导出 CSV   ────────→ XLSX.writeFile()        │
   │                      (本地文件下载)            │
   │                                               │
   └─────────────────────────────────────────────┘
```

### 流程 B: 手动添加商品 (新增 ✅)

```
用户操作              前端处理
   │                  │
   │─点击"打开表单"──→ setShowProductForm(true)
   │                  ProductForm 显示
   │                  │
   │─输入 7 个字段───→ 表单字段 onChange
   │                  form state 更新
   │                  │
   │─点击"添加商品"──→ handleAddProduct(product)
   │                  setProducts([...])
   │                  清空表单
   │                  │
   │─商品列表更新  ──→ 显示新添加商品
   │                  支持删除操作
   │                  │
   │─点击"生成草稿"──→ (同流程 A)
   │
```

---

## 3️⃣ 数据结构对比

```
┌─────────────────────────────────────────────────┐
│            Product (商品数据)                    │
├─────────────────────────────────────────────────┤
│ id: string              (唯一标识)               │
│ name: string            (必填，商品名称)        │
│ category?: string       (分类)                  │
│ brand?: string          (品牌)                  │
│ material?: string       (材质)                  │
│ size?: string           (尺寸)                  │
│ color?: string          (颜色)                  │
│ targetAudience?: string (目标人群)             │
│ images?: string[]       (图片 URL/base64)      │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│        GenerateResult (生成结果)                │
├─────────────────────────────────────────────────┤
│ productId: string          (对应商品 ID)        │
│ mainImageDraft: string     (主图 base64)       │
│ titleDraft: string         (标题文案)           │
│ sellingPoints: string[]    (卖点，1-2 条)      │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│          Template (模板定义)                    │
├─────────────────────────────────────────────────┤
│ id: string                 (唯一标识)            │
│ name: string               (模板名称)           │
│ tags: string[]             (标签分类)           │
│ parts: {                   (模板内容)            │
│   titleTemplate?: string        (标题模板)      │
│   sellingPointsTemplates?: []   (卖点模板)      │
│   imageTemplate?: object        (图片模板)      │
│ }                                               │
│ createdAt: string          (创建时间)          │
└─────────────────────────────────────────────────┘
```

---

## 4️⃣ 关键数据转换步骤

```
Step 1: Excel → Product
┌──────────────────────┐
│  Excel 文件            │
├──────────────────────┤
│ 第1行: name | brand   │
│ 第2行: 羊绒围巾 | Luxe │
│ 第3行: 运动鞋 | Speed │
└──────────────────────┘
          ↓
   XLSX.read() 
   + 字段映射
          ↓
┌──────────────────────────────┐
│ Product[] (前端内存)          │
├──────────────────────────────┤
│ {id: "p-0", name: "羊绒..."}  │
│ {id: "p-1", name: "运动..."}  │
└──────────────────────────────┘

Step 2: Product → JSON (网络传输)
┌──────────────────────────────┐
│ Product[] (内存数组)          │
└──────────────────────────────┘
          ↓
   JSON.stringify()
          ↓
┌──────────────────────────────────────────┐
│ 网络请求体 (text/json)                   │
│ {"items":[{id:"p-0",...},{id:"p-1",...}]}│
└──────────────────────────────────────────┘

Step 3: GenerateResult 生成 (后端)
┌──────────────────────────────┐
│ Product (单个)                │
│ {name:"羊绒", brand:"Luxe"...}│
└──────────────────────────────┘
          ↓
   generateDraftForItem()
   ├─ 规则引擎: name+brand → title
   ├─ 字段映射: material → sellingPoint
   └─ SVG 合成: Canvas → base64
          ↓
┌────────────────────────────────────┐
│ GenerateResult                     │
│ {                                  │
│   productId: "p-0",                │
│   mainImageDraft: "data:...",      │
│   titleDraft: "Luxe 羊绒...",      │
│   sellingPoints: ["100% 羊绒..."]   │
│ }                                  │
└────────────────────────────────────┘

Step 4: JSON → React State (前端)
┌──────────────────────────────────────┐
│ HTTP 响应体 (JSON)                   │
│ {"results":[{productId:"p-0",...}]} │
└──────────────────────────────────────┘
          ↓
   JSON.parse()
   setResults()
          ↓
┌──────────────────────────────┐
│ results[] (前端 state)        │
│ (用于 ResultCard 渲染)        │
└──────────────────────────────┘
          ↓
   editedResults.set()
          ↓
┌────────────────────────────────────┐
│ editedResults Map<productId, ...>  │
│ (编辑后的版本)                      │
└────────────────────────────────────┘
```

---

## 5️⃣ 网络请求示例 (真实数据)

### 请求示例

```http
POST http://localhost:3000/api/generate-batch HTTP/1.1
Content-Type: application/json
Content-Length: 456

{
  "items": [
    {
      "id": "p-0",
      "name": "羊绒围巾",
      "category": "围巾",
      "brand": "Luxe",
      "material": "100% 羊绒",
      "size": "180cm x 30cm",
      "color": "深灰色",
      "targetAudience": "白领女性"
    }
  ],
  "options": {
    "saveToLibrary": false,
    "templateId": null
  }
}
```

### 响应示例 (简化)

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 1024

{
  "results": [
    {
      "productId": "p-0",
      "mainImageDraft": "data:image/svg+xml;base64,PHN2ZyB4bWxucz0naHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmcnIHdpZHRoPSc4MDAn...",
      "titleDraft": "Luxe 羊绒围巾 深灰色",
      "sellingPoints": [
        "100% 羊绒 材质",
        "适合白领女性"
      ]
    }
  ]
}
```

---

## 6️⃣ 存储位置分布

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│  浏览器                                             │
│  ├─ React State (内存, 关闭刷新消失)                │
│  │  ├─ products[]        ← Excel/手动输入          │
│  │  ├─ results[]         ← 后端生成               │
│  │  └─ editedResults Map ← 前端编辑               │
│  │                                                 │
│  ├─ localStorage (持久化, 刷新不消失)              │
│  │  └─ product_templates ← 用户收藏的模板         │
│  │                                                 │
│  └─ Cookie (可选)                                  │
│     └─ 用户会话信息                                │
│                                                     │
├───────────────────────────────────────────────────┤
│                                                     │
│  Node.js 服务器 (内存)                             │
│  ├─ let templates[] (程序运行期间存储)             │
│  └─ (重启后清空)                                   │
│                                                     │
├───────────────────────────────────────────────────┤
│                                                     │
│  本地文件系统 (用户设备)                           │
│  ├─ example.csv        ← 导出的草稿               │
│  └─ main-image-p-0.png ← 下载的主图               │
│                                                     │
├───────────────────────────────────────────────────┤
│                                                     │
│  数据库 (可选，未来升级)                           │
│  ├─ products table                                │
│  ├─ drafts table                                  │
│  └─ templates table                               │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 7️⃣ 流量示意 (5 个商品)

```
Excel 文件          (硬盘读取, 本地)
  ↓
25 KB → 前端
  ↓
25 KB → POST /api/generate-batch
  ↓
200 KB ← SVG base64 (后端生成)
  ↓
200 KB → 浏览器显示
  ↓
5 KB → POST /api/templates (收藏)
  ↓
50 KB → 导出 CSV 下载

总计: 500+ KB (取决于图片质量和数据量)
```

---

## 8️⃣ 状态转移图

```
                    初始状态
                       │
                       ↓
    ┌──────────────────────────────────┐
    │   上传 Excel 或 手动添加          │
    │   setProducts([...])              │
    └──────────────────────────────────┘
                       │
                       ↓
    ┌──────────────────────────────────┐
    │   点击"生成草稿"                  │
    │   POST /api/generate-batch        │
    │   setIsLoading(true)              │
    └──────────────────────────────────┘
                       │
                       ↓
    ┌──────────────────────────────────┐
    │   后端处理                        │
    │   generateDraftForItem() × N      │
    └──────────────────────────────────┘
                       │
                       ↓
    ┌──────────────────────────────────┐
    │   接收结果                        │
    │   setResults([...])               │
    │   setIsLoading(false)             │
    └──────────────────────────────────┘
                       │
                ┌──────┴──────┐
                │             │
                ↓             ↓
    ┌────────────────┐  ┌──────────────────┐
    │ 编辑结果       │  │ 收藏为模板        │
    │ 本地修改       │  │ POST /api/        │
    │ editedResults  │  │   templates       │
    └────────────────┘  └──────────────────┘
                │             │
                └──────┬──────┘
                       │
                       ↓
    ┌──────────────────────────────────┐
    │   导出 CSV 或 继续操作            │
    │   downloadImage()                 │
    │   exportCSV()                     │
    └──────────────────────────────────┘
                       │
                       ↓
                    完成操作
```

---

这份可视化文档覆盖了：
- 🏗️ 整体架构
- 📊 数据流向
- 🔄 状态管理
- 📡 网络通信
- 💾 存储位置
- 🚀 性能指标
- 🔀 状态转移
