[package]
name = "balltree"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.26", features = ["extension-module"] }
core = { path = "../core", features = ["simd"] }
ordered-float = "4.2"
ndarray = "0.16"

[lib]
name = "balltree"
crate-type = ["cdylib"]
