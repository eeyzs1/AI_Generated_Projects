[package]
name = "pq"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.26", features = ["extension-module"] }
core = { path = "../core" }
rand = "0.8"

[lib]
name = "pq"
crate-type = ["cdylib"]
