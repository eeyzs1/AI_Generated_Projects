apisix:
  node_listen: 9080
  enable_ipv6: false
  config_center: yaml

deployment:
  role: traditional
  role_traditional:
    config_provider: yaml

plugin_attr:
  log-rotate:
    enable_compression: false
